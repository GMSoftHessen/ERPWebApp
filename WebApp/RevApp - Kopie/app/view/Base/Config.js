
Ext.define('App.view.base.Config', {
    extend: 'Ext.panel.Panel',
    xtype: 'bas-config',
   
    _: function () {
        //var store = Ext.getStore('locales')
        //    , rec = store.findRecord('textId', textId)
        //    ;
        return 'eeeeee';//rec ? rec.get(MyApp.currentLocale) : text;
    }
  
});
